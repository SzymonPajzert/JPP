list = [1,2,3];

test1 = match list with {
  | [a, b, c] -> (a+b+c) = 6
  | _         -> False
  };

test2 = match list with {
  | h :: tail -> h = 1
  | _         -> False
  };

test3 = match list with {
  | _ :: h :: tail -> h = 2
  | _         -> False
  };

main = test1 && test2 && test3;